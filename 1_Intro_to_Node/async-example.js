// requires word is used to import functions , modules

const distance = require("./square");
console.log(
  `Distance Area is : ${distance.area(
    4
  )}& Perimeter distance ${distance.perimeter(12)}`
);
